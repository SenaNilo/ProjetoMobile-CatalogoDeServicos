import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  picker: {
    height: 50,
    color: '#004173',
    margin: 20,
    borderRadius: 5
  },
});

export default styles;
