import { StyleSheet } from 'react-native'

const styles = new StyleSheet.create({
  input:{
    width: "100%",
    padding: 10,
    borderWidth: 1,
    borderColor: '#222',
    borderRadius: 10,
    marginBottom: 10,
  }
})

export default styles