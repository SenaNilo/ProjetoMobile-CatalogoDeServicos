import { StyleSheet } from 'react-native'

const styles = new StyleSheet.create({
  textAlign:{
    alignSelf: 'center'
  },
  text:{
    marginTop: 10,
    marginBottom: 5,
    fontWeight: 600,
  }
}
)

export default styles